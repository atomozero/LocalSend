# Binari di de-risking compilati su Haiku
tools/check/test_multicast
tools/check/test_openssl
tools/check/test_tcp_http
tools/check/test_json
# Header JSON vendorizzato a parte (scaricarlo localmente)
tools/check/json.hpp

# Certificati di prova
*.pem

# Build
build/
*.o
/localsend-send
/localsend-receive
/LocalSend
/LocalSendDeskbar
/LocalSendDesktop
/Send with LocalSend
*.rsrc
test-receive-bin
test-board-bin

# Pacchetti e archivi generati
*.hpkg
*.zip

# Cataloghi compilati (artefatti: build da locales/*.catkeys con `make catalogs`)
data/locale/catalogs/
locales/_pre.cpp

# Fingerprint generato a runtime
localsend_fingerprint

# Stato runtime (impostazioni, cronologia, preferiti, registro bacheca)
localsend_settings
localsend_history
localsend_favorites
localsend_board_seen

# File ricevuti a runtime (cartella di default del ricevente)
/ricevuti/
